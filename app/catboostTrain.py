# from catboost import CatBoostClassifier

# model = CatBoostClassifier(
#     loss_function='MultiClass',
#     iterations=500,
#     depth=6,
#     learning_rate=0.1,
#     cat_features=['soil_type', 'region', 'season']  # auto-handled
# )
# model.fit(X_train, y_train)